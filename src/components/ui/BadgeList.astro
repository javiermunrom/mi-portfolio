---
interface Props {
  items: string[];
}

const { items } = Astro.props;
---

<ul class="flex flex-wrap gap-3">
  {
    items.map((item) => (
      <li class="badge-chip">{item}</li>
    ))
  }
</ul>
